# Same backend as customer storefront / staff (FastAPI).
EXPO_PUBLIC_API_ORIGIN=http://127.0.0.1:8000
EXPO_PUBLIC_API_PREFIX=/api/v1

# After `eas init`, paste project ID here for EAS Update / cloud builds:
# EAS_PROJECT_ID=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
