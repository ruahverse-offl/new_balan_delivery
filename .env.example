# Copy to .env — same variables as new_balan_fe
VITE_API_BASE_URL=https://devapi.newbalanpharmacy.com
VITE_API_PREFIX=/api/v1

# Android emulator → host machine (alternative)
# VITE_API_BASE_URL=http://10.0.2.2:8000

# Optional overrides (Flutter names)
# API_ORIGIN=https://devapi.newbalanpharmacy.com
# API_PREFIX=/api/v1
